# Documentation archive

This directory preserves workshop drafts and superseded research documents. These files
are historical evidence, not current product or architecture contracts.

Do not extend an archived document during unrelated work. Update the relevant current
page under `docs/` and link to an archive file only when its historical context matters.

Archive directories remain thematic and contain no more than five direct files.
